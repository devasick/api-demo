extends layout

block content
  h1= title
  p Welcome to #{title}
  form(name="add-estimation", method="post" action="/quotes")
        div.input
            span.label Name
            input(type="text", name="name")
        div.input
            span.label Quote
            input(type="text", name="quote")
        div.actions
            input(type="submit", value="add")
